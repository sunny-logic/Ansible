---
  - name: Run Gotify
    hosts: "{{hostnames}}"
    become: true
    tasks:
    - name: Run manage_gotify
      shell:
         "{{python_bin}} {{manage_gotify_script}}"
         #"/bin/python3 /data/scripts/python/manage_gotify/manage_gotify.py"
